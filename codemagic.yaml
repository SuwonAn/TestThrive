workflows:
  ios_free_build:
    name: iOS Free Build
    max_build_duration: 60

    environment:
      xcode: latest

    scripts:
      - name: Build iOS app (unsigned)
        script: |
          xcodebuild \
            -project "TestThrive.xcodeproj" \
            -scheme "TestThrive" \
            -configuration Release \
            -sdk iphoneos \
            build

    artifacts:
      - build/**/*.app
